# 节奏按摩引导器 · 核心 PRD（双垫版 v3）

> **目标设备**：两块硅胶纹理垫，双手各执一块，每块可完整覆盖单侧乳晕+乳头。
> **一句话**：一款全自动、零操作的手机网页应用：以双硅胶垫为工具，按"引诱→蓄力→攀爬→释放→回落"的螺旋上升模型生成编排，用强弱节拍音引导模式切换与活动状态，亮屏常亮播放，结束留档。

**v3 修订摘要**（相对 v2）：

- 休息升级为**第五模式"静置"**，与四动作模式地位平等；静置并入回落段作为后半段（乳晕摩擦 → 静置），不再以独立休息块存在于单元之外。
- 单侧无动作（引诱/攀爬中另侧）定名为 **"空置"**，与双侧"静置"概念分离：空置是对比机制，静置是阈值归零。
- 回落段改为两段式：乳晕摩擦（有接触零快感，减速过渡）+ 静置（无接触无声音，神经归零）。
- 核心段 1:2 配比改为：静置合计 : 有动作合计 ≈ 1:2，编译器按核心时长反算静置总量后均分至各单元回落段。
- 冲刺段规则收紧：同时禁止乳晕摩擦与静置。
- 蓄力段与回落段中乳晕摩擦的屏幕显示加方向标识（↗升温 / ↘降温），避免同名混淆。

---

## 1. 核心流程（唯一主链路）

```
设置总时长 → 生成编排 → 预览序列 → 开始 → 全自动播放（热身→核心→冲刺→收尾）
        → 静默着陆 → 评分/收藏 → 历史落盘 → 复盘
```

---

## 2. 触摸模型（物理基础）

### 2.1 五模式定义

| 模式       | 快感强度  | 接触面积  | 运动      | 功能角色                 |
| -------- | ----- | ----- | ------- | -------------------- |
| 摩擦乳晕周围   | 无     | 环形/线状 | 慢速画圈    | 过渡 / 减速（蓄力时升温、回落时降温） |
| 轻触       | 弱     | 点（瞬时） | 轻触即离    | 引诱 / 建立预期            |
| 点按-松开    | 中     | 点（持续） | 按下停留再松开 | 聚焦刺激 / 敏化维持          |
| 全覆盖滑动/旋转 | 强     | 面（持续） | 滑动/旋转   | 释放 / 高潮触发            |
| **静置**   | **无** | **无** | **无**   | **归零 / 阈值复位**        |

- 摩擦乳晕与静置同为"零快感"，但功能不同：摩擦乳晕保持身体连接，静置彻底断开让神经复位。
- 静置始终双侧同步执行，不存在单侧静置。

### 2.2 活动状态

任一时刻，编排处于三种活动状态之一：

| 状态   | 左侧  | 右侧  | 快感  | 消耗  | 用途            |
| ---- | --- | --- | --- | --- | ------------- |
| 双侧同步 | 同模式 | 同模式 | 最强  | 高   | 蓄力、释放、回落（含静置） |
| 左单侧  | 有动作 | 空置  | 中   | 低   | 引诱、攀爬——对比与追逐  |
| 右单侧  | 空置  | 有动作 | 中   | 低   | 同上（镜像）        |

- **"空置"**：垫子静置原位，不给任何刺激。空置是引诱/攀爬段中的**对比机制**（"有 vs 无"制造预期张力），与静置（双侧归零）是不同概念。
- 单侧阶段里动作的一侧称**主导侧**；相邻单元主导侧必须轮换。
- **交错**（仅用于波峰）：左右单侧沿时间轴严格交替，永远恰好一侧有动作、一侧空置。

### 2.3 节拍音的强弱语义

节拍音同时传递**模式指令**与**活动状态**：

- **重拍（低音鼓点/心跳）** = 全覆盖滑动/旋转
- **中拍（指尖敲击）** = 点按-松开
- **轻拍（水滴）** = 轻触
- **连音（木鱼）** = 乳晕摩擦
- **静默** = 静置（被主动选择的"静默音色"，非"没东西播放"）
- **声道规则**：
  - 双侧同步 → 双耳播放统一节拍（同音色同 BPM，听感居中）
  - 单侧活动 → **仅对应耳出声**（左单侧 = 只有左耳响），另一耳完全静音——注意力唯一指向
  - 静置 → 双耳静默
  - 波峰交错 → 双耳各自独立节奏，左右反相错峰

---

## 3. 功能需求

### 3.1 编排（首页）

- 总时长设定：1～60 分钟（滑块 + 常用预设值）。
- 「生成编排」→ 一次性生成完整序列。
- 预览：统计概览（总时长 / 刺激总时长 / 静置总时长 / 总单元数，热身 N 单元 → 核心 N 单元 → 冲刺段）+ 序列列表（每单元各阶段时长、BPM、活动状态、主导侧）。
- 「重新编排」（随机性体现在：各阶段时长微调 ±20%、滑动方向、每单元主导侧左/右）、「开始播放」。

### 3.2 播放器（全自动，零操作，亮屏）

- 点击开始后无需任何触碰；屏幕显示**当前阶段名称 + 圆形倒计时 + 活动状态徽标（双侧/左侧/右侧/静置）+ 主导侧指示（L/R，单侧时段显示）**。
- 乳晕摩擦在屏幕上的阶段名加方向标识：蓄力段显示"乳晕摩擦 ↗"，回落段显示"乳晕摩擦 ↘"，避免同名混淆。
- 按序列自动播放节拍音，声道规则见 §2.3；只面向立体声耳机，开始播放前一次性提示"请佩戴左右声道耳机"，不做外放适配。
- **亮屏常亮播放**：开始时申请屏幕常亮（Wake Lock），全程亮屏显示；手动锁屏/切后台自动暂停，回到页面 2 小时内可询问"继续/放弃"。
- 用户可以选择扣下手机实现暂停，翻转继续播放（加速度计判定）；扣下时保持亮屏、界面渐隐全黑防漏光，翻回来自动恢复。
- 进度实时保存。

### 3.3 结束落地

- 收尾后进入**静默着陆**：呼吸灯动画 60 秒（前 30 秒心跳音逐渐减速至静默，后 30 秒完全静默），可提前结束。
- 可选评分/收藏 → 历史自动落盘 → 返回首页。

### 3.4 历史与收藏

- 历史：最近 20 次，列表展示（时长 / 单元数 / 评分 / 时间），可删除，可「加载此编排」再播。
- 收藏：命名列表、删除；可加载再播。

### 3.5 设置

- 四档速度自定义：慢 60 / 中 105 / 快 120 / 极速 135 BPM（40～200 可调），各档音色可选，实时试听。
- 乳晕缓冲时长：10～30 秒，作为每个单元**回落段中乳晕摩擦部分**的时长区间（系统在此范围内随机，敏感用户设区间偏高，经验用户偏低）。静置时长不由用户控制，由编译器按 1:2 配比自动计算。
- 收尾音色。
- 偏好记忆：常用时长下次默认填入。
- 数据管理：清空全部数据（二次确认）。

---

## 4. 核心领域规则（编排引擎）

### 4.1 阶段结构（按总时长分配）

| 阶段   | 占比     | 内容                                           |
| ---- | ------ | -------------------------------------------- |
| 热身   | 前 20%  | 双侧同步乳晕摩擦 + 双侧同步轻触交替，慢速 60 BPM，建立接触基线；不含静置    |
| 核心   | 中间 60% | 螺旋单元串联，每单元含静置（回落后半段）；核心段内 静置合计 : 有动作合计 ≈ 1:2 |
| 冲刺   | 最后 20% | 起冲→加速→波峰（交替单侧）→高位持续→收尾；禁止乳晕摩擦与静置             |
| 静默着陆 | 收尾后    | 60 秒渐进静默（前 30s 心跳减速 + 后 30s 完全静默），呼吸灯动画      |

### 4.2 核心单元模板

每个单元 ≈ 2.5-3.5 分钟（含静置），五个阶段固定顺序，回落段内部分为前后两段：

```
阶段    时长      活动状态                        节拍特征
────────────────────────────────────────────────────────────────
引诱    25-35s   左单侧/右单侧（主导）轻触          仅主导耳轻拍
                另侧空置                         "有 vs 无"对比制造预期张力

蓄力    20-30s   双侧同步 乳晕摩擦 ↗ → 点按 渐进     双耳连音 → 中拍渐入
                从零感过渡到聚焦，双侧敏化一致启动

攀爬    30-40s   左单侧/右单侧（主导）全覆盖         仅主导耳重拍
                另侧空置                         主导侧独自爬升，制造"追逐感"

释放    40-60s   双侧同步 全覆盖滑动/旋转           双耳统一重拍
                双侧汇合峰值                      求和效应最大化

回落 ┌─ 前段    10-30s   双侧同步 乳晕摩擦 ↘         双耳统一连音
     │                 从高强度平滑过渡，保持连接
     │
     └─ 后段    （计算值） 双侧同步 静置              双耳静默
                      无接触无声音，神经阈值归零
```

**主导侧轮换**：奇数单元左侧主导，偶数单元右侧主导；该单元的引诱段与攀爬段共用同一主导侧。

**静置时长计算**：

- 乳晕摩擦部分 = 乳晕缓冲区间（10-30s）内随机。
- 静置部分 = 编译器按"核心段静置合计 : 有动作合计 ≈ 1:2"反算静置总量，均分到各单元回落段。
- 仅 1 个单元时，全部静置配比集中在该单元回落段内。
- 有动作 = 四模式（轻触/点按/全覆盖/乳晕摩擦）；空置（引诱/攀爬中的单侧无动作）不计入休息，算有动作的一部分。

### 4.3 螺旋递进规则

核心段串联 N 个单元（容量 = 有动作总时长 ÷ 3 分钟），三维递进：

| 单元          | 释放时长    | 释放 BPM  | 主导侧  |
| ----------- | ------- | ------- | ---- |
| 单元 1        | 40s     | 105     | 左    |
| 单元 2        | 50s     | 120     | 右    |
| 单元 3        | 60s     | 135     | 左    |
| 单元 4        | 50s     | 135     | 右    |
| 单元 5+（封顶循环） | 60s（封顶） | 135（封顶） | 左右交替 |

约束：

- 释放时长逐单元递增 10-15s **或** BPM 提高一档（封顶后维持最高档）。
- 单元间必经 回落（乳晕摩擦 ↘ → 静置）→ 下一引诱，禁止从全覆盖或乳晕摩擦直接跳入引诱段。
- 每个单元的主导侧与上一单元相反。

### 4.4 高潮冲刺段结构

```
起冲 (40s)      双侧同步 全覆盖滑动 20s，105 BPM
                → 左单侧 10s → 右单侧 10s（两侧行权热身）

加速 (50s)      双侧同步 全覆盖旋转
                BPM 105 → 120 线性递增
                每 10s 插入一次 2s"左右快闪"：
                左单侧 1s → 右单侧 1s → 即刻恢复双侧
                （微闪避，防双侧同步麻木）

波峰 (60s)      左右交替单侧全覆盖：
                左 6s → 右 6s → 左 6s → ……
                永远恰好一侧在全覆盖、一侧空置
                每侧 6s 动 / 6s 空置
                双耳反相各自节奏，BPM 135

高位持续 (60s)  双侧同步 全覆盖慢速滑动
                BPM 120 → 105 渐降

收尾 (15s)      双侧同步 轻触 3-5 次
                → 双侧乳晕摩擦 5s → 移开
```

**错峰原理**：波峰不追求"双侧同时最强"（容易双侧同时麻木），而是两侧轮流在全覆盖和空置之间切换，整体快感是一条无断点的连续高线，每侧却有周期性空置恢复。

**冲刺段禁止乳晕摩擦与静置**：冲刺全程不下基线，恢复仅靠波峰的单侧空置交替。收尾段的乳晕摩擦是唯一例外——它在冲刺结束后、静默着陆前执行，属于收尾缓冲。

### 4.5 七条硬性规则

1. **单元内阶段顺序固定**：引诱→蓄力→攀爬→释放→回落（乳晕摩擦 ↘ → 静置），不可跳过或重排。随机性仅体现于各阶段时长 ±20% 和释放阶段滑动方向。
2. **引诱必须单侧**：主导侧轻触，另侧空置，禁止双侧同动引诱——"有 vs 无"的对比是预期张力的来源。
3. **回落必须双侧同步**：先乳晕摩擦 ↘（至少 10s）后静置，双侧同步执行。禁止从释放直接进入静置——必须经乳晕摩擦过渡。
4. **高潮冲刺禁止乳晕摩擦与静置**：冲刺段全程在三个快感模式间切换，不下基线。恢复仅靠波峰的单侧空置交替。收尾段乳晕摩擦是冲刺结束后的缓冲，不属冲刺本体。
5. **主导侧必须轮换**：相邻单元的主导侧必须不同，确保两侧轮流获得"被追逐"的心理关注。
6. **静置只存在于核心段回落内**：双侧完全无动作、双耳静默；核心段内静置 : 有动作 ≈ 1:2，由编译器按配比自动计算静置总时长并均分至各单元。
7. **总时长精确匹配**：各单元时长之和 + 阶段过渡（每次 3s）+ 冲刺段 = 设定总时长（误差 ±5 秒）。

### 4.6 中断恢复

- 播放进度实时写入 `sessionStorage`；手动锁屏/切后台自动暂停；再次打开若检测到未完成编排（2 小时内有效），弹窗询问继续或放弃；正常结束或放弃后清除。

### 4.7 声音信号

| 事件              | 信号                                   |
| --------------- | ------------------------------------ |
| 每个模式阶段          | 对应节拍音（见 §4.8）；双侧同步双耳同拍、单侧仅对应耳、静置双耳静默 |
| 引诱最后 5 秒        | 主导侧轻拍渐强——预告即将进入蓄力                    |
| 攀爬最后 5 秒        | 主导侧重拍渐强渐密——预告双侧汇合释放                  |
| 释放最后 5 秒        | 重拍渐慢渐轻——预告回落                         |
| 回落前段 → 后段（进入静置） | 连音 2 秒内淡出至静默                         |
| 静置结束前 3 秒       | 一声轻"叮"预告恢复                           |
| 热身 → 核心         | 一声"叮——"，音色库整体切换                      |
| 核心 → 冲刺         | 两声"叮叮"，进入低音鼓点域                       |
| 单侧 → 双侧汇合       | 一声柔和"叮"（引诱→蓄力、攀爬→释放、快闪回双侧时）          |
| 冲刺 → 收尾         | 节拍停止，静默 1 秒                          |
| 收尾 → 静默着陆       | 心跳音从 60 BPM 逐渐减速至 0，持续 30s 后静默       |

### 4.8 速度档位、音色与模式映射

| 档位  | 默认 BPM  | 默认音色   | 对应模式   | 用途                   |
| --- | ------- | ------ | ------ | -------------------- |
| 慢速  | 60      | 水滴     | 轻触     | 引诱、收尾                |
| 慢速  | 60      | 木鱼     | 乳晕摩擦   | 蓄力前段（↗）、回落前段（↘）、收尾缓冲 |
| 中速  | 80-105  | 指尖敲击   | 点按-松开  | 蓄力后段                 |
| 快速  | 105-120 | 心跳     | 全覆盖滑动  | 释放、起冲、高位持续           |
| 极速  | 135     | 低音鼓点   | 全覆盖旋转  | 波峰                   |
| 收尾  | 3 秒/音   | 经典嗒音   | 轻触（轻柔） | 收尾缓冲                 |
| —   | —       | **静默** | **静置** | **回落后段（阈值归零）**       |

7 种音色（Web Audio 实时合成、零依赖、无音频文件）：经典嗒音 / 木鱼 / 心跳 / 水滴 / 指尖敲击 / 低音鼓点 / 静默（静置专用，被主动选择的无声）。

### 4.9 声道规范

- 双侧同步：双耳统一节拍，同音色同 BPM，听感居中。
- 单侧活动：仅对应耳播放（该侧模式的音色 + BPM），**另一耳完全静音**——声音只指向正在动作的那只手。
- 静置：双耳静默。
- 波峰交错：双耳各自独立节奏，左右反相错峰（一耳响时另一耳静）。
- 转场"叮"类信号：双耳同时播放。

### 4.10 存储

- 偏好 → localStorage；历史 / 收藏 → IndexedDB；播放进度 → sessionStorage。纯本地、无云端。
